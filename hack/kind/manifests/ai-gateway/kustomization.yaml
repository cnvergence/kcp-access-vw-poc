apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - certificate.yaml
  - mcp-route.yaml
  - proxy-service.yaml
  - keycloak-backend.yaml
  # reference-grant.yaml kept for documentation but MCPRoute doesn't support cross-namespace refs
